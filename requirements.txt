# Pinned so the bundled third-party libraries (the heavy "deps" update layer)
# stay byte-stable across releases. Bump deliberately when upgrading.
PyQt6==6.11.0
PyQt6-Qt6==6.11.1
PyQt6-sip==13.11.1
Pillow==12.2.0
opencv-python-headless==4.11.0.86
numpy==2.4.6
